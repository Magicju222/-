# 🇨🇳 中国大陆访问特别指南

您问到了关键问题。实话实说：**Streamlit Cloud 和 Render 在中国大陆的访问速度很不稳定，甚至可能被墙。**

如果您的用户主要在中国大陆，且没有 VPN，建议采用以下两种方案：

## 方案一：本地运行 + 内网穿透 (最推荐个人/小团队)
这是**零成本、无需服务器、国内访问速度快**的方案。
原理：代码在您电脑上运行，通过工具生成一个公网链接，发给同事即可访问。

### 步骤
1.  **保持本地运行**: 确保您的终端里 `streamlit run app.py` 正在运行。
2.  **下载工具**: 推荐使用 **[cpolar](https://www.cpolar.com/)** (国内最流行，免费) 或 **[natapp](https://natapp.cn/)**。
3.  **映射端口**:
    *   安装并登录 cpolar。
    *   在命令行输入: `cpolar http 8501`
4.  **获取链接**: cpolar 会生成一个类似 `http://xyz.cpolar.cn` 的链接。
5.  **分享**: 将这个链接发给国内用户，他们即可流畅访问。

---

## 方案二：香港云服务器 (最推荐生产环境)
如果您需要一个长期稳定、24小时在线的地址，建议购买一台 **香港 (Hong Kong)** 地区的云服务器 (VPS)。

### 🌏 为什么这是最佳方案？(双向互通)
这是一个**"完美桥梁"**方案：
1.  **国内访问 (快)**: 香港服务器对中国大陆线路有特殊优化 (CN2/GIA)，国内用户访问它就像访问国内网站一样快，且**不需要备案**。
2.  **海外访问 (通)**: 香港服务器属于国际互联网，海外用户访问毫无障碍。
3.  **Supabase 连接 (稳)**: 关键点！由于您的代码运行在香港服务器上，它与 Supabase (海外) 的连接是**服务器对服务器**的内部通信，完全不受国内防火墙干扰。用户只需要能连上您的香港服务器，就能顺畅使用所有功能。

*   **推荐厂商**: 阿里云 (Aliyun HK)、腾讯云 (Tencent HK)。费用约 20-30 元/月。

### 部署步骤 (Docker 一键部署)
1.  **购买服务器**: 选择 CentOS 或 Ubuntu 系统，安装 Docker。
2.  **上传代码**: 将项目文件上传到服务器。
3.  **配置环境变量**: 在服务器项目目录下创建 `.env` 文件：
    ```bash
    SUPABASE_URL=您的SupabaseURL
    SUPABASE_KEY=您的SupabaseKey
    ```
4.  **启动**:
    ```bash
    docker-compose up -d
    ```
5.  **访问**: 打开浏览器访问 `http://服务器IP:8501`。

---

## ⚠️ 关于 Supabase 的连接性
本项目使用的 **Supabase** (数据库/认证) 服务器在海外。
*   **大多情况下**: 国内网络可以直接连接 Supabase API，只是速度稍慢。
*   **极少数情况**: 如果您的公司内网屏蔽了 Supabase，登录功能可能会报错。
    *   *解决方案*: 如果遇到此问题，建议在服务器端部署一套开源的 Supabase (难度较大)，或改用国内的类似服务 (如 MemFire Cloud)。但通常情况下，直接连接是没问题的。

## 总结
*   **演示/临时使用**: 选 **方案一 (cpolar)**。
*   **长期使用**: 选 **方案二 (香港服务器)**。
*   **不要使用**: Streamlit Cloud / Render / Vercel (国内体验较差)。
