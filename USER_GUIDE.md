# AI Excel 清洗助手 - 用户使用手册

欢迎使用 **AI Excel 清洗助手 (AI Excel Cleaner)**。这是一款专为处理“中国式复杂报表”设计的专业工具，支持多用户云端协作。

---

## 1. 核心能力与规格

### ✅ 支持的文件格式
*   **`.xlsx` (推荐)**：现代 Excel 格式。支持最完整的**物理合并单元格自动识别**功能。
*   **`.xls`**：Excel 97-2003 格式。
*   **`.csv`**：逗号分隔值文件。

### 📏 数据规模限制
*   **文件大小**：单个文件最大支持 **200MB**（Streamlit 默认限制）。
*   **数据行数**：
    *   **预览模式**：为了保证流畅性，界面上只加载并显示前 **1000 行** 数据供您进行结构选择。
    *   **实际清洗**：**无行数限制**。后台会对整个文件进行全量处理。只要服务器内存足够，几十万行甚至上百万行数据均可处理。

### 🛡️ 安全性
*   **企业级认证**：通过 Supabase Auth 提供安全的用户注册与登录保护，确保您的使用环境隔离。
*   **数据处理**：文件上传与清洗过程在服务器内存中完成，**不持久化存储原始文件**，保障数据隐私。

---

## 2. 快速开始

### 部署与配置
如果您是管理员需要部署，请参考 [部署指南](DEPLOY.md)。
普通用户请直接访问部署好的 URL。

---

## 3. 详细使用教程

### 第一步：登录/注册
首次使用请先注册账号。
1.  输入邮箱和密码。
2.  点击 **Sign Up** 注册（可能需要邮箱验证，视管理员配置而定）。
3.  已有账号请直接 **Sign In**。

### 第二步：上传文件
登录成功后，将您的 Excel 或 CSV 文件拖拽到首页的**上传区域**。
> **提示**：现在的上传区域采用了透明毛玻璃设计，并带有动态激光流动效果。当文件拖入时，边缘会高亮显示。

### 第三步：选择工作表与定义结构
文件上传后，您会看到一个**工作表选择器**。

1.  **选择 Sheet**：勾选您想要清洗的工作表（默认全选）。
2.  **配置每个 Sheet**：
    *   在下方的**标签页 (Tabs)** 中，点击切换不同的 Sheet。
    *   为**每一个 Sheet** 单独定义表头行和数据开始行（操作方式同上：点击行变黄/绿）。
    *   如果某个 Sheet 结构不同，您可以单独调整，互不影响。

### 第四步：处理合并单元格/空值填充（关键列）
这是解决“合并单元格”导致数据丢失的关键步骤。

*   **场景**：比如“地区”列，"华东"两个字是个大合并单元格，涵盖了下面 5 行。在 Excel 读取时，只有第 1 行有"华东"，下面 4 行通常是空的。
*   **操作**：在预览表格中，**点击该列的标题**（例如点击列头 `A` 或 `B`）。
*   **效果**：该列会被标记为“关键列”。清洗时，系统会自动对该列执行**向下填充 (Fill Down)** 操作，把空值填满，确保每一行数据都有完整的地区信息。

### 第五步：开始清洗与下载
1.  （可选）在左侧侧边栏的“高级设置”中，您可以修改**多级表头分隔符**（默认为 ` / `，例如 `华东 / 上海`）。
2.  点击页面下方的 **“开始批量智能清洗”** 按钮。
3.  清洗完成后，右侧会显示结果预览（支持多 Sheet 切换查看）。点击 **“下载清洗后的数据”** 即可获得包含所有已清洗 Sheet 的 `.xlsx` 文件。

---

## 4. 常见问题 (FAQ)

**Q: 为什么预览里只看到前 1000 行？我的数据有 1 万行。**
A: 为了防止浏览器卡顿，预览仅展示前 1000 行供您定义结构。请放心，点击“开始清洗”后，后台会对**所有数据**进行处理。

**Q: `.xls` 和 `.csv` 文件也能处理合并单元格吗？**
A: 可以。虽然这两种格式不包含物理合并单元格信息（读取时已经是打散的空值），但您依然可以通过**第三步（点击列标题）**来强制执行“向下填充”，从而完美修复数据。

**Q: 为什么我的列名变成了 `Total`, `Total_1`？**
A: 如果原表中存在重复的表头（例如两个“合计”），系统为了保证数据列名的唯一性，会自动添加 `_1`, `_2` 后缀。

**Q: 怎么切换语言？**
A: 左侧侧边栏顶部有语言切换选项，支持“简体中文”和“English”。

---

## 5. 管理员功能

如果您是管理员，登录后会在侧边栏看到 **"🔧 管理面板"** 按钮。

### 5.1 用户管理
*   查看所有注册用户列表
*   搜索和筛选用户（按邮箱、角色、状态）
*   封禁/解封用户
*   修改用户角色（user/admin/super_admin）
*   批量操作支持

### 5.2 清洗日志
*   查看所有用户的清洗操作记录
*   按时间范围筛选
*   查看处理详情和错误信息
*   导出日志数据

### 5.3 数据分析
*   用户注册趋势图表
*   清洗任务统计
*   系统使用概览
*   时间范围筛选

### 5.4 系统配置
*   **基本设置**：维护模式、用户注册开关、邮箱验证
*   **文件设置**：最大文件大小、允许的文件类型、最大行数限制
*   **显示设置**：系统公告（支持 HTML）
*   **维护设置**：数据清理周期
*   **高级设置**：统计分析开关、API 速率限制

> **提示**：管理面板数据每 30 秒自动缓存，切换标签页时会快速加载。点击"🔄 刷新数据"按钮可立即获取最新数据。

---

## 6. 注意事项
*   **环境要求**：建议使用 Python 3.8 及以上版本。
*   **特殊字符**：清洗过程中，系统会自动去除表头中的换行符、特殊控制符，以确保生成的列名干净可用。
*   **管理员权限**：需要管理员手动在数据库中设置用户角色为 `admin` 或 `super_admin`。
