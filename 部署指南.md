# 🚀 部署指南：让项目上线 (公网访问)

您提到想使用 Vercel 部署。虽然 Vercel 非常优秀，但它主要用于 Next.js/React 等前端框架。对于 **Streamlit** 这种基于 Python 的数据应用，它需要一个持续运行的后台，而 Vercel 的 Serverless 架构并不支持。

因此，要达到 **"免费、一键托管、公网访问"** 的效果，**Streamlit Community Cloud** 是官方推荐且唯一的完美替代方案 (体验和 Vercel 几乎一样)。

---

## 方案 A：Streamlit Community Cloud (推荐 ⭐⭐⭐⭐⭐)
这是最简单、最快的方式。

### 第一步：上传代码到 GitHub
1.  在 GitHub 上创建一个新仓库 (例如 `ai-excel-cleaner`)。
2.  将您本地的所有代码上传到该仓库。
    *   **注意**: 您的 `.streamlit/secrets.toml` 文件 (包含 Supabase 密钥) **不会** 被上传 (已被 `.gitignore` 保护)，这是为了安全。

### 第二步：一键部署
1.  访问 [share.streamlit.io](https://share.streamlit.io/) 并使用 GitHub 账号登录。
2.  点击右上角的 **"New app"**。
3.  选择您刚才创建的 GitHub 仓库 (`ai-excel-cleaner`)。
4.  **关键步骤：配置密钥**
    *   在部署界面的 **"Advanced settings"** (高级设置) 中，找到 **"Secrets"** 区域。
    *   将您本地 `.streamlit/secrets.toml` 中的内容复制进去：
        ```toml
        SUPABASE_URL = "您的 Supabase URL"
        SUPABASE_KEY = "您的 Supabase Key"
        ```
5.  点击 **"Deploy!"**。

### 第三步：大功告成
等待约 1-2 分钟，您将获得一个类似 `https://ai-excel-cleaner.streamlit.app` 的永久公网链接。您可以将此链接分享给任何人，他们即可通过 Supabase 账号登录使用。

---

## 方案 B：Render (Docker 部署)
如果您需要更强的控制力 (例如自定义 Docker 环境)，可以使用 Render。

1.  在 [Render.com](https://render.com) 注册账号。
2.  点击 **"New +"** -> **"Web Service"**。
3.  连接您的 GitHub 仓库。
4.  选择 **"Docker"** 作为 Runtime。
5.  在 **"Environment Variables"** 中添加：
    *   Key: `SUPABASE_URL`, Value: (您的 URL)
    *   Key: `SUPABASE_KEY`, Value: (您的 Key)
6.  点击 **"Create Web Service"**。

---

## 为什么不能用 Vercel?
Vercel 是 "Serverless" (无服务器) 架构，意味着它只在有请求时运行几秒钟代码，然后立即冻结。
*   **Streamlit 的需求**: Streamlit 需要与用户建立 WebSocket 长连接来保持界面状态。
*   **冲突**: Vercel 会切断这个长连接，导致应用出现 "Connection Lost" 或无限重连，根本无法使用。

**Streamlit Cloud** 本质上就是 "专门为 Streamlit 定制的 Vercel"，它是免费且官方支持的。
